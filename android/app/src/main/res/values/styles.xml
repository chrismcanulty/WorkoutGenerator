<resources>

    <!-- Base application theme. -->
    <style name="splashScreenTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/workout_icon_360x360</item>
    </style>

</resources>
